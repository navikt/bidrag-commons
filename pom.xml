<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE project [
  <!ELEMENT project (modelVersion|groupId|artifactId|version|packaging|description|properties|scm|dependencyManagement|dependencies|repositories|distributionManagement|build)*>
  <!ATTLIST project
    xmlns CDATA #REQUIRED
    xmlns:xsi CDATA #REQUIRED
    xsi:schemaLocation CDATA #REQUIRED>
  <!ELEMENT modelVersion (#PCDATA)>
  <!ELEMENT groupId (#PCDATA)>
  <!ELEMENT artifactId (#PCDATA)>
  <!ELEMENT version (#PCDATA)>
  <!ELEMENT packaging (#PCDATA)>
  <!ELEMENT description (#PCDATA)>
  <!ELEMENT properties (project.build.sourceEncoding|project.reporting.outputEncoding|logback-encoder.version|spring-boot.version|java.version|maven-compiler.version|maven-surefire.version)*>
  <!ELEMENT project.build.sourceEncoding (#PCDATA)>
  <!ELEMENT project.reporting.outputEncoding (#PCDATA)>
  <!ELEMENT logback-encoder.version (#PCDATA)>
  <!ELEMENT spring-boot.version (#PCDATA)>
  <!ELEMENT java.version (#PCDATA)>
  <!ELEMENT maven-compiler.version (#PCDATA)>
  <!ELEMENT maven-surefire.version (#PCDATA)>
  <!ELEMENT scm (url|developerConnection)*>
  <!ELEMENT url (#PCDATA)>
  <!ELEMENT developerConnection (#PCDATA)>
  <!ELEMENT dependencyManagement (dependencies)*>
  <!ELEMENT dependencies (dependency)*>
  <!ELEMENT dependency (groupId|artifactId|version|type|scope|exclusions)*>
  <!ELEMENT type (#PCDATA)>
  <!ELEMENT scope (#PCDATA)>
  <!ELEMENT exclusions (exclusion)*>
  <!ELEMENT exclusion (groupId|artifactId)*>
  <!ELEMENT repositories (repository)*>
  <!ELEMENT repository (id|name|url|snapshots)*>
  <!ELEMENT id (#PCDATA)>
  <!ELEMENT name (#PCDATA)>
  <!ELEMENT snapshots (enabled)*>
  <!ELEMENT enabled (#PCDATA)>
  <!ELEMENT distributionManagement (repository|snapshotRepository)*>
  <!ELEMENT snapshotRepository (id|name|url|uniqueVersion)*>
  <!ELEMENT uniqueVersion (#PCDATA)>
  <!ELEMENT build (plugins)*>
  <!ELEMENT plugins (plugin)*>
  <!ELEMENT plugin (artifactId|version|groupId|configuration)*>
  <!ELEMENT configuration (release)*>
  <!ELEMENT release (#PCDATA)>
  ]>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>no.nav.bidrag</groupId>
  <artifactId>bidrag-commons</artifactId>
  <version>0.0.28-SNAPSHOT</version>
  <packaging>jar</packaging>

  <description>Bibliotek for komponenter som brukes på tvers av applikasjoner i no.nav.bidrag</description>

  <properties>
    <!-- build -->
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

    <!-- dependencies -->
    <logback-encoder.version>5.3</logback-encoder.version>
    <spring-boot.version>2.1.5.RELEASE</spring-boot.version>

    <!-- language -->
    <java.version>12</java.version>

    <!-- plugins -->
    <maven-compiler.version>3.8.0</maven-compiler.version>
    <maven-surefire.version>2.22.0</maven-surefire.version>
  </properties>

  <scm>
    <url>https://github.com/navikt/bidrag-commons</url>
    <developerConnection>scm:git:https://github.com/navikt/bidrag-commons</developerConnection>
  </scm>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
      <version>${spring-boot.version}</version>
      <exclusions>
        <exclusion>
          <groupId>com.fasterxml.jackson</groupId>
          <artifactId>jackson-databind</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <dependency>
      <groupId>ch.qos.logback</groupId>
      <artifactId>logback-core</artifactId>
    </dependency>

    <dependency>
      <groupId>ch.qos.logback</groupId>
      <artifactId>logback-classic</artifactId>
    </dependency>

    <dependency>
      <groupId>net.logstash.logback</groupId>
      <artifactId>logstash-logback-encoder</artifactId>
      <version>${logback-encoder.version}</version>
      <exclusions>
        <exclusion>
          <groupId>com.fasterxml.jackson</groupId>
          <artifactId>jackson-databind</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <!-- test -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-api</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-engine</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-params</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- dependency to overwrite vulnerable dependency of jackson-databind -->
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <version>2.9.9</version>
    </dependency>
  </dependencies>

  <repositories>
    <repository>
      <id>local-releases</id>
      <name>Local stuff</name>
      <url>https://repo.adeo.no/repository/maven-releases</url>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </repository>
  </repositories>

  <distributionManagement>
    <repository>
      <id>m2internal</id>
      <name>NAV Maven2 Internal Repository</name>
      <url>http://maven.adeo.no/nexus/content/repositories/m2internal</url>
    </repository>
    <snapshotRepository>
      <id>m2snapshot</id>
      <name>NAV Maven2 Snapshot Repository</name>
      <url>http://maven.adeo.no/nexus/content/repositories/m2snapshot</url>
      <uniqueVersion>false</uniqueVersion>
    </snapshotRepository>
  </distributionManagement>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>${maven-surefire.version}</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${maven-compiler.version}</version>
        <configuration>
          <release>${java.version}</release>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>
